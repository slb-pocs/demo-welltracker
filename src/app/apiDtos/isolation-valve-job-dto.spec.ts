import { IsolationValveJobDto } from './isolation-valve-job-dto';

describe('IsolationValveJobDto', () => {
  it('should create an instance', () => {
    expect(new IsolationValveJobDto()).toBeTruthy();
  });
});
