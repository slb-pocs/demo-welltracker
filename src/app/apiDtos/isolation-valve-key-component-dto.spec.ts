import { IsolationValveKeyComponentDto } from './isolation-valve-key-component-dto';

describe('IsolationValveKeyComponentDto', () => {
  it('should create an instance', () => {
    expect(new IsolationValveKeyComponentDto()).toBeTruthy();
  });
});
