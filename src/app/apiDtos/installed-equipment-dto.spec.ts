import { InstalledEquipmentDto } from './installed-equipment-dto';

describe('InstalledEquipmentDto', () => {
  it('should create an instance', () => {
    expect(new InstalledEquipmentDto()).toBeTruthy();
  });
});
