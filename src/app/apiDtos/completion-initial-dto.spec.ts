import { CompletionInitialDto } from './completion-initial-dto';

describe('CompletionInitialDto', () => {
  it('should create an instance', () => {
    expect(new CompletionInitialDto()).toBeTruthy();
  });
});
