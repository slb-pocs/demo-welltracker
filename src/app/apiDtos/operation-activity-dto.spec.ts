import { OperationActivityDto } from './operation-activity-dto';

describe('OperationActivityDto', () => {
  it('should create an instance', () => {
    expect(new OperationActivityDto()).toBeTruthy();
  });
});
