import { GetSurfaceEquipmentDto } from './get-surface-equipment-dto';

describe('GetSurfaceEquipmentDto', () => {
  it('should create an instance', () => {
    expect(new GetSurfaceEquipmentDto()).toBeTruthy();
  });
});
