import { SurfaceEquipmentDto } from './surface-equipment-dto';

describe('SurfaceEquipmentDto', () => {
  it('should create an instance', () => {
    expect(new SurfaceEquipmentDto()).toBeTruthy();
  });
});
