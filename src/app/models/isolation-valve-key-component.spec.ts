import { IsolationValveKeyComponent } from './isolation-valve-key-component';

describe('IsolationValveKeyComponent', () => {
  it('should create an instance', () => {
    expect(new IsolationValveKeyComponent()).toBeTruthy();
  });
});
