import { UppercompletionType } from './uppercompletion-type';

describe('UppercompletionType', () => {
  it('should create an instance', () => {
    expect(new UppercompletionType()).toBeTruthy();
  });
});
