import { IsolationValveJobType } from './isolation-valve-job-type';

describe('IsolationValveJobType', () => {
  it('should create an instance', () => {
    expect(new IsolationValveJobType()).toBeTruthy();
  });
});
