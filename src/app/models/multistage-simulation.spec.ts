import { MultistageSimulation } from './multistage-simulation';

describe('MultistageSimulation', () => {
  it('should create an instance', () => {
    expect(new MultistageSimulation()).toBeTruthy();
  });
});
