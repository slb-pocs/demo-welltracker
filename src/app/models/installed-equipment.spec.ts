import { InstalledEquipment } from './installed-equipment';

describe('InstalledEquipment', () => {
  it('should create an instance', () => {
    expect(new InstalledEquipment()).toBeTruthy();
  });
});
