import { ArtificialliftType } from './artificiallift-type';

describe('ArtificialliftType', () => {
  it('should create an instance', () => {
    expect(new ArtificialliftType()).toBeTruthy();
  });
});
