import { CompletionInitialData } from './completion-initial-data';

describe('CompletionInitialData', () => {
  it('should create an instance', () => {
    expect(new CompletionInitialData()).toBeTruthy();
  });
});
