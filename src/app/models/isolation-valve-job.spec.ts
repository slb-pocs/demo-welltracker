import { IsolationValveJob } from './isolation-valve-job';

describe('IsolationValveJob', () => {
  it('should create an instance', () => {
    expect(new IsolationValveJob()).toBeTruthy();
  });
});
