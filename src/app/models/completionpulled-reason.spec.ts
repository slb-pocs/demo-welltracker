import { CompletionpulledReason } from './completionpulled-reason';

describe('CompletionpulledReason', () => {
  it('should create an instance', () => {
    expect(new CompletionpulledReason()).toBeTruthy();
  });
});
